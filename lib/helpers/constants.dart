import 'package:flutter/material.dart';

// APP
const String appName = 'My App';
const String appVersion = '1.0.0';
const String appAuthor = 'Akram Qasem';

// Colors
const Color primaryColor = Color(0xFFFF5F0C);
const Color secondaryColor = Color(0xFF007AFF);
const Color backgroundColor = Color(0xFFF5F5F5);
const Color textColor = Color(0xFF000000);

// Fonts

// Sizes
const double defaultSize = 15.0;
const double defaultPadding = 15.0;
const double defaultMargin = 15.0;
const double defaultRadius = 15.0;
const double defaultShadow = 5.0;
